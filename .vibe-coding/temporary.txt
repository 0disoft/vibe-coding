# Vibe Coding 프로젝트 아키텍처 분석 보고서

## 1. 현재 구조 진단

### 1.1 현재 폴더 구조

```
src/
├── lib/
│   ├── components/       # UI 컴포넌트 (혼합)
│   ├── constants/        # ★ 신규 상수 통합
│   ├── prefs/            # ⚠️ deprecated (상수 중복)
│   ├── stores/           # Svelte 스토어
│   ├── types/            # 타입 정의
│   ├── config.ts         # ⚠️ deprecated
│   ├── theme.svelte.ts   # ⚠️ deprecated
│   └── font-size.svelte.ts # ⚠️ deprecated
├── routes/
│   ├── [[lang]]/         # 다국어 라우트
│   │   ├── cookie/
│   │   ├── privacy/
│   │   └── terms/
│   └── offline/
└── styles/               # 글로벌 CSS
```

### 1.2 발견된 문제점

| 영역 | 문제 | 심각도 |
|------|------|--------|
| **상수 관리** | deprecated 파일이 여러 곳에 산재 (`config.ts`, `prefs/`, `font-size.svelte.ts`, `theme.svelte.ts`) | 🔴 높음 |
| **컴포넌트 구조** | 모든 컴포넌트가 평면적으로 나열됨 (레이아웃, UI, 비즈니스 로직 혼합) | 🔴 높음 |
| **라우트 구조** | 정책 페이지만 존재, 향후 대규모 확장 시 구조화 방안 부재 | 🟡 중간 |
| **유틸리티 부재** | 공통 유틸리티 함수 폴더가 없음 | 🟡 중간 |
| **서비스 레이어 부재** | API 통신, 비즈니스 로직 분리 구조 없음 | 🟡 중간 |
| **테스트 구조** | 테스트 파일이 소스와 혼합 배치 | 🟢 낮음 |

---

## 2. 확장성을 위한 권장 구조

### 2.1 제안하는 폴더 구조

```
src/
├── lib/
│   ├── components/           # UI 컴포넌트 (기능별 분리)
│   │   ├── common/           # 재사용 공통 컴포넌트
│   │   │   ├── Button.svelte
│   │   │   ├── Modal.svelte
│   │   │   ├── Toast.svelte
│   │   │   └── index.ts
│   │   ├── layout/           # 레이아웃 전용 컴포넌트
│   │   │   ├── Header.svelte
│   │   │   ├── Footer.svelte
│   │   │   ├── Sidebar.svelte
│   │   │   └── index.ts
│   │   ├── forms/            # 폼 관련 컴포넌트
│   │   │   ├── Input.svelte
│   │   │   ├── Select.svelte
│   │   │   └── index.ts
│   │   └── features/         # 기능별 특화 컴포넌트
│   │       ├── cart/         # 장바구니
│   │       ├── checkout/     # 결제
│   │       ├── blog/         # 블로그
│   │       └── search/       # 검색
│   │
│   ├── constants/            # 상수 (현재 유지)
│   │   ├── index.ts
│   │   ├── cookies.ts
│   │   ├── site.ts
│   │   ├── policy.ts
│   │   └── routes.ts         # ★ 신규: 라우트 경로 상수화
│   │
│   ├── stores/               # 전역 상태 관리
│   │   ├── index.ts
│   │   ├── theme.svelte.ts
│   │   ├── font-size.svelte.ts
│   │   ├── cart.svelte.ts    # ★ 향후: 장바구니 상태
│   │   └── user.svelte.ts    # ★ 향후: 사용자 상태
│   │
│   ├── services/             # ★ 신규: API/비즈니스 로직 레이어
│   │   ├── api/              # API 통신
│   │   │   ├── client.ts     # fetch wrapper
│   │   │   ├── products.ts
│   │   │   └── orders.ts
│   │   ├── auth/             # 인증 로직
│   │   └── payment/          # 결제 로직
│   │
│   ├── utils/                # ★ 신규: 유틸리티 함수
│   │   ├── index.ts
│   │   ├── format.ts         # 날짜, 통화 포맷
│   │   ├── validation.ts     # 유효성 검증
│   │   └── helpers.ts        # 범용 헬퍼
│   │
│   ├── types/                # 타입 정의 (유지)
│   │   ├── index.ts
│   │   ├── api.ts            # API 응답 타입
│   │   ├── models.ts         # 도메인 모델
│   │   └── components.ts     # 컴포넌트 Props
│   │
│   └── index.ts              # 메인 배럴 파일
│
├── routes/                   # SvelteKit 라우트
│   ├── (marketing)/          # ★ 그룹: 마케팅/랜딩 페이지
│   │   ├── about/
│   │   ├── features/
│   │   ├── pricing/
│   │   └── contact/
│   │
│   ├── (legal)/              # ★ 그룹: 법적 문서
│   │   ├── terms/
│   │   ├── privacy/
│   │   └── cookie/
│   │
│   ├── (content)/            # ★ 그룹: 콘텐츠 페이지
│   │   ├── blog/
│   │   ├── guides/
│   │   ├── faq/
│   │   └── changelog/
│   │
│   ├── (shop)/               # ★ 그룹: 이커머스
│   │   ├── products/
│   │   ├── cart/
│   │   ├── checkout/
│   │   └── orders/
│   │
│   ├── (dashboard)/          # ★ 그룹: 대시보드 (인증 필요)
│   │   ├── +layout.svelte    # 인증 가드
│   │   ├── settings/
│   │   └── billing/
│   │
│   └── [[lang]]/             # 다국어 래퍼 (현재 유지)
│
└── styles/                   # 글로벌 CSS (유지)
```

### 2.2 핵심 변경 사항

#### ✅ 즉시 실행 권장 (우선순위 1)

1. **deprecated 파일 정리**
   - `src/lib/config.ts` → 삭제 (이미 constants로 이전됨)
   - `src/lib/theme.svelte.ts` → 삭제 (stores로 이전됨)
   - `src/lib/font-size.svelte.ts` → 삭제 (stores로 이전됨)
   - `src/lib/prefs/` → 폴더 삭제 (constants로 이전됨)

2. **components 폴더 분리**
   - `components/layout/` 생성 → Header.svelte, Footer.svelte 이동
   - `components/common/` 생성 → 재사용 UI 컴포넌트
   - `components/ui/` → `components/common/`으로 병합 또는 명확히 역할 구분

3. **utils 폴더 신설**
   - 공통 유틸리티 함수 관리 시작

#### ✅ 중기 실행 권장 (우선순위 2)

4. **services 레이어 도입**
   - API 통신 로직 분리
   - 비즈니스 로직과 UI 분리

5. **라우트 그룹화**
   - SvelteKit의 `(group)` 폴더 기능 활용
   - 관련 페이지끼리 그룹핑하여 레이아웃 공유

6. **타입 정의 확장**
   - 도메인 모델 타입 세분화
   - API 응답 타입 정의

---

## 3. 네이밍 컨벤션 검토

### 3.1 현재 파일명 검토

| 현재 | 문제점 | 권장 |
|------|--------|------|
| `persisted-state.svelte.ts` | kebab-case와 dot 혼용 | ✅ 유지 (Svelte 관례) |
| `header-actions/` | 역할 불명확 | `header/actions/` 또는 `header/` 하위 정리 |
| `CodeBlock.svelte` | 레이아웃과 혼재 | `components/common/` 이동 |

### 3.2 권장 네이밍 규칙

```
폴더명: kebab-case (예: header-actions)
컴포넌트: PascalCase (예: CodeBlock.svelte)
유틸/서비스: camelCase (예: formatDate.ts)
상수 파일: kebab-case (예: route-paths.ts)
타입 파일: kebab-case (예: api-types.ts)
```

---

## 4. 협업 효율성을 위한 추가 권장사항

### 4.1 배럴 파일(index.ts) 전략

```typescript
// components/layout/index.ts
export { default as Header } from './Header.svelte';
export { default as Footer } from './Footer.svelte';

// 사용
import { Header, Footer } from '$lib/components/layout';
```

### 4.2 경로 별칭 추가 권장

```typescript
// svelte.config.js
alias: {
  '$components': 'src/lib/components',
  '$stores': 'src/lib/stores',
  '$services': 'src/lib/services',
  '$utils': 'src/lib/utils',
  '$types': 'src/lib/types',
}
```

### 4.3 코드 소유권(CODEOWNERS) 도입

```
# 향후 .github/CODEOWNERS 예시
/src/lib/components/     @frontend-team
/src/lib/services/       @backend-team
/src/routes/(shop)/      @ecommerce-team
/messages/               @i18n-team
```

---

## 5. 향후 기능별 배치 가이드

| 기능 | 권장 라우트 | 권장 컴포넌트 위치 |
|------|-------------|-------------------|
| About, Contact | `(marketing)/` | `components/features/marketing/` |
| Blog, Guides, News | `(content)/` | `components/features/blog/` |
| Products, Cart, Checkout | `(shop)/` | `components/features/shop/` |
| FAQ, Support | `(support)/` | `components/features/support/` |
| Dashboard, Billing | `(dashboard)/` | `components/features/dashboard/` |
| Search | 글로벌 또는 `(content)/` | `components/common/SearchBar.svelte` |

---

## 6. 마이그레이션 체크리스트

### Phase 1: 정리 (즉시)
- [ ] deprecated 파일 4개 삭제
- [ ] `prefs/` 폴더 삭제
- [ ] `utils/` 폴더 생성 및 배럴 파일 추가
- [ ] `components/layout/` 폴더 생성, Header/Footer 이동

### Phase 2: 구조화 (1주 내)
- [ ] `components/common/` 폴더 정리
- [ ] `services/` 폴더 생성
- [ ] 라우트 그룹 `(marketing)/`, `(legal)/` 적용

### Phase 3: 확장 준비 (기능 추가 시)
- [ ] 각 feature별 컴포넌트 폴더 생성
- [ ] API 서비스 레이어 구현
- [ ] 도메인 타입 정의 확장

---

## 7. 결론

현재 프로젝트는 소규모 개발에 적합한 구조이지만, **무한 확장과 다인 협업**을 가정하면:

1. **수직적 분리** (레이어별): lib → components/stores/services/utils/types
2. **수평적 분리** (기능별): features/shop, features/blog 등
3. **라우트 그룹화**: SvelteKit의 (group) 문법 활용

이 세 가지 전략을 병행하면 팀원 간 충돌을 최소화하고, 각 도메인별 독립적 개발이 가능해집니다.

---

*작성일: 2025-12-08*
*작성자: Antigravity AI Assistant*
