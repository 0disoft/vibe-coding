# REVIEW

각 작업을 완료하기 전, 아래 항목을 빠르게 점검하고 최종 코드를 확정하십시오.

## 1. 문서 동기화 (필수)

- **[SPEC 업데이트]** 새로운 페이지나 라우트가 생성되었는가?
  - 👉 그렇다면 `.vibe-coding/SPEC.md`의 `3. Core Features & Route Map` 섹션에 **URL**과 **핵심 파일 경로**를 반드시 추가했는가?
- **[API 최신화]** API 엔드포인트나 데이터 모델(Interface)이 변경되었는가?
  - 👉 그렇다면 `.vibe-coding/API.md`에 변경된 사항들을 반영했는가?
- **[TODO 갱신]** `.vibe-coding/TODO.md`에서 현재 작업의 상태를 업데이트했는가?

## 2. 주석 (한국어/상세)

- **[언어 및 톤]** 모든 주석은 **상세한 한국어**로 작성되었는가?
- **[내용의 질]** 단순한 동작 설명이 아닌, **"왜 이렇게 짰는지(Why)"**에 대한 의도가 충분히 설명되었는가?

## 3. 구조 및 안정성

- **[동기/비동기 적합성]** I/O(DB, 파일, 네트워크) 작업은 반드시 **비동기(Async/Await)**로 처리하여 블로킹을 방지했는가? 반대로, 단순 연산에 불필요한 Await를 사용하지는 않았는가?
- **[모듈화]** 코드가 비대해지지 않도록 기능 단위로 쪼개고, **최소 단위로 모듈화**했는가?
- **[예외 처리]** API 호출이나 DB 접근 등 실패 가능한 지점에 `try-catch` 처리가 되어 있는가?
- **[타입 정의]** `any` 타입 사용을 최소화하고, 데이터 구조에 맞는 인터페이스를 정의했는가?

## 4. 스타일 (테마 준수)

- **[색상 토큰 사용]** `#hex`나 `rgb()` 같은 하드코딩된 색상 값을 쓰지 않았는가?
  - 👉 반드시 설정 파일(UnoCSS Config)에 정의된 **테마 색상 변수**만 사용해야 한다.

## 5. 보안

- **[환경변수]** API Key나 Secret Token이 코드에 하드코딩되지 않았는가?
