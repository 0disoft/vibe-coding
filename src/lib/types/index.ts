/**
 * $lib/types Barrel Export (타입 전용)
 *
 * 이 파일은 $lib/types 경로로 import 할 수 있는 타입의 진입점입니다.
 * User, AuthSession 등 비즈니스 로직 타입을 여기서 re-export 하세요.
 *
 * 사용 예시:
 * import type { User, AuthSession } from '$lib/types';
 *
 * ⚠️ 런타임 코드(함수, 스토어 등)는 $lib/index.ts 를 사용하세요.
 * ⚠️ app.d.ts에서 사용할 타입은 여기서 정의 후 import 하세요.
 */

// 아래에 타입 export 추가
// export type { User } from './user';
// export type { AuthSession } from './auth';
